# Core dependencies
openai>=1.0.0
pyyaml>=6.0
python-dotenv>=1.0.0

# Performance optimization (optional but recommended)
msgpack>=1.0.0  # 3-5x faster cache serialization

# Optional dependencies for testing
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-cov>=4.0.0
